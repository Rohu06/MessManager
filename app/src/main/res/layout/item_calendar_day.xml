<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clickable="true"
    android:focusable="true">

    <!-- Tinted circular status background -->
    <View
        android:id="@+id/viewStatusCircle"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_calendar_day_circle"
        android:visibility="invisible" />

    <!-- Today highlight ring (only visible for today) -->
    <View
        android:id="@+id/viewTodayRing"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_calendar_today_ring"
        android:visibility="invisible" />

    <!-- Day number -->
    <TextView
        android:id="@+id/tvDayNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:fontFamily="sans-serif-medium"
        android:textSize="14sp"
        tools:text="22" />

</FrameLayout>